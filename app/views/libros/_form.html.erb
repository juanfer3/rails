<%= form_with(model: libro, local: true) do |form| %>
  <% if libro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(libro.errors.count, "error") %> prohibited this libro from being saved:</h2>

      <ul>
      <% libro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titulo %>
    <%= form.text_field :titulo, id: :libro_titulo %>
  </div>

  <div class="field">
    <%= form.label :autor_id %>
    <%= form.text_field :autor_id, id: :libro_autor_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
